<?php
$title = $this->translate('Departamento');
$this->headTitle($title);
?>
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="row">
            <div class="col-md-6">
                <h1 class="panel-title"><i class="glyphicon glyphicon-suitcase"></i><?php echo $this->escapeHtml($title); ?></h1>
            </div>
            <div class="col-md-6">
                <div class="btn-group btn-group-sm pull-right">
                    <a class="btn btn-default" href="<?php echo $this->url('admin/department/add', array('action' => 'add')); ?>"> <i class="glyphicon glyphicon-plus"></i> <?php echo $this->translate('Novo'); ?></a>
                </div>
            </div> 
        </div>
    </div>
    <table class="table table-condensed table-hover">
        <tr>
            <th class="" colspan="10"><?php echo $this->translate('DEPARTAMENTO'); ?></th>
        </tr>
        <?php if (0 == count($this->department)): ?>
            <tr>
                <td colspan="10" class="alert alert-info text-center"><?php echo $this->translate('Não há registros no banco de dados.') ?></td>
            </tr>
        <?php else: ?>
            <?php foreach ($this->department as $department) : ?>
                <tr>
                    <td><?php echo $this->escapeHtml($department->getDepartmentName()); ?></td>
                    <td>
                        <div class="btn-group btn-group-xs pull-right">
                            <a class="btn btn-default" href="<?php echo $this->url('admin/department/edit', array('action' => 'edit', 'id' => $department->getDepartmentId()))
                ?>"><i class="glyphicon glyphicon-edit"></i></a>
                            <a class="btn btn-default" href="<?php echo $this->url('admin/department/delete', array('action' => 'delete', 'id' => $department->getDepartmentId()))
                ?>"><i class="glyphicon glyphicon-remove"></i></a>
                        </div>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
        </tbody>
    </table>
    <div class="panel-footer">
        <?php
        $paginationInfo = $this->placeholder('pagination');
        if (!isset($paginationInfo->route)) {
            $paginationInfo->route = 'admin/department';
            $paginationInfo->params = array();
        }
        ?>
        <?php echo $this->paginationControl($this->department, 'Sliding', 'layout/pagination'); ?>
    </div>
</div>
