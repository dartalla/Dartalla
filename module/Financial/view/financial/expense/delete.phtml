<?php
$title = $this->translate('Apagar Despesa');
$this->headTitle($title);
$url = $this->url('admin/financial/expense/delete', array(
    'action' => 'delete',
    'id'     => $this->id,
));

?>
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="panel-title"><i class="fa fa-upload-alt"></i> <?php echo $this->escapeHtml($title); ?></div>
    </div>
    <div class="panel-body">
        <blockquote>
            <p><?php echo $this->translate('Você tem certeza de que deseja apagar ') ?>
                '<?php echo $this->escapeHtml($this->expense->getLaunch()->getLaunchDescription()); ?>'?
            </p>
        </blockquote>
        <form action="<?php echo $url; ?>" method="post">
            <div>
                <input type="hidden" name="id" value="<?php echo $this->expense->getExpenseId(); ?>" />
                <div class="btn-group">
                    <input  type="submit" name="del" value="Sim" class="btn btn-primary" />
                    <input type="submit" name="del" value="Não" class="btn btn-default" />
                </div>
            </div>
        </form>
    </div>
</div>
