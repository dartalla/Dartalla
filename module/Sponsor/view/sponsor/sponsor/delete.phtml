<?php
$title = $this->translate('Apagar Avalista / Cônjuge');
$this->headTitle($title);
$url = $this->url('admin/sponsor/delete', array(
    'action' => 'delete',
    'id' => $this->id,
        ));
?>
<div class="panel panel-default">
    <div class="panel-heading">
        <h2 class="panel-title"><?php echo $this->escapeHtml($title); ?></h2>
    </div>
    <div class="panel-body">
        <blockquote><?php echo $this->translate('Você tem certeza de que deseja apagar ') ?>
            '<?php echo $this->escapeHtml($this->sponsor->getPerson()->getPersonName()); ?>'?
        </blockquote>
        <form action="<?php echo $url; ?>" method="post">
            <input type="hidden" name="id" value="<?php echo $this->sponsor->getSponsorId(); ?>" />
            <div class="btn-group">
                <input type="submit" name="del" value="Sim" class="btn btn-default" />
                <input type="submit" name="del" value="Não" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>
