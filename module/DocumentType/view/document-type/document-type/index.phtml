<?php
$title = $this->translate('Tipos de Documentos');
$this->headTitle($title);
?>
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="row">
            <div class="col-md-6">
                <h1 class="panel-title"><i class="fa fa-file-text"></i> <?php echo $this->escapeHtml($title); ?></h1>
            </div>
            <div class="col-md-6">
                <div class="btn-group btn-group-sm pull-right">
                    <a class="btn btn-default" href="<?php echo $this->url('admin/document-type/add', array('action' => 'add')); ?>"> <i class="glyphicon glyphicon-plus"></i> <?php echo $this->translate('Novo'); ?></a>
                </div>
            </div> 
        </div>
    </div>
    <table class="table table-condensed table-hover">
        <tr>
            <th class="" colspan="10"><?php echo $this->translate('TIPO DE DOCUMENTO'); ?></th>
        </tr>
        <?php if (0 == count($this->documentType)): ?>
            <tr>
                <td colspan="10" class="alert alert-info text-center"><?php echo $this->translate('Não há registros no banco de dados.') ?></td>
            </tr>
        <?php else: ?>
            <?php foreach ($this->documentType as $documentType) : ?>
                <tr>
                    <td><?php echo $this->escapeHtml($documentType->getDocumentTypeName()); ?></td>
                    <td>
                        <div class="btn-group btn-group-xs pull-right">
                            <a class="btn btn-default" href="<?php echo $this->url('admin/document-type/edit', array('action' => 'edit', 'id' => $documentType->getDocumentTypeId()))
                ?>"><i class="glyphicon glyphicon-edit"></i></a>
                            <a class="btn btn-default" href="<?php echo $this->url('admin/document-type/delete', array('action' => 'delete', 'id' => $documentType->getDocumentTypeId()))
                ?>"><i class="glyphicon glyphicon-remove"></i></a>
                        </div>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
        </tbody>
    </table>
    <div class="panel-footer">
        <?php
        $paginationInfo = $this->placeholder('pagination');
        if (!isset($paginationInfo->route)) {
            $paginationInfo->route = 'admin/document-type';
            $paginationInfo->params = array();
        }
        ?>
        <?php echo $this->paginationControl($this->documentType, 'Sliding', 'layout/pagination'); ?>
    </div>
</div>
